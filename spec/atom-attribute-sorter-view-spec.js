'use babel';

import AtomAttributeSorterView from '../lib/atom-attribute-sorter-view';

describe('AtomAttributeSorterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
